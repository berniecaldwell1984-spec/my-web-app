import React, { useState } from "react";

const MaterialTestModal = ({ onClose, onSave }) => {
  const [name, setName] = useState("");
  const [type, setType] = useState("");

  const handleSave = () => {
    if (!name.trim()) return;
    const newTest = {
      id: Date.now(),
      name: name.trim(),
      type: type.trim(),
    };
    onSave(newTest);
    onClose();
  };

  return (
    <div className="fixed inset-0 bg-black bg-opacity-60 flex items-center justify-center z-50">
      <div className="bg-gray-900 p-6 rounded shadow-lg w-96 text-white">
        <h2 className="text-xl mb-4">Add Material Test</h2>

        <div className="mb-3">
          <label className="block mb-1 text-sm">Name</label>
          <input
            className="w-full p-2 bg-gray-800 text-white rounded"
            value={name}
            onChange={(e) => setName(e.target.value)}
          />
        </div>

        <div className="mb-3">
          <label className="block mb-1 text-sm">Type</label>
          <input
            className="w-full p-2 bg-gray-800 text-white rounded"
            value={type}
            onChange={(e) => setType(e.target.value)}
          />
        </div>

        <div className="flex justify-end mt-4 gap-2">
          <button
            className="bg-gray-700 px-4 py-2 rounded"
            onClick={onClose}
            type="button"
          >
            Cancel
          </button>
          <button
            className="bg-blue-600 px-4 py-2 rounded"
            onClick={handleSave}
            type="button"
          >
            Save
          </button>
        </div>
      </div>
    </div>
  );
};

// placeholder text file - intentionally left blank to avoid accidental imports
// This file was created accidentally. No JS code should be placed here.
